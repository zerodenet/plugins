# 仓库工作指南

[English](AGENTS.md) · **简体中文**

## 分支职责

修改前阅读 `README.md`、`CONTRIBUTING.md` 和 `docs/governance.md`。`main` 仅包含平台介绍和公共文档；插件源码、打包脚本和运行时 CI 属于对应的 `zboard` 或 `znet-sink` 分支。编辑前先确认当前分支。

宿主分支的源码采用 `<host>/<plugin>/` 目录，保留插件 ID、发布者身份、公开模块路径和历史提交。宿主实现与公开 SDK 留在各自仓库，不创建嵌套 Git 仓库。

## 架构

各宿主负责能力检查、核心业务状态、私有存储和生命周期事务。插件使用专用、带版本的宿主 API，不引入数据库访问、任意宿主命令、管理员令牌或节点凭据。共享市场不意味着通用运行时或全局权限。

## 文档

英文为默认语言，同步维护 `.zh-CN.md`、双向语言链接以及一致的命令和配置示例。跨分支链接应明确指定目标分支。工作站信息和任务报告留在公共指南之外，提案标记为草案。

## 验证

`main` 检查文档链接、语言配对、示例，以及不存在跟踪中的实现文件。`zboard` 运行 `sh scripts/check.sh`，打包变更还需验证签名开发包。`znet-sink` 在实现提供专属检查前验证文档，不提前声称客户端运行时已有测试覆盖。

通过 `--zboard` 或 `ZBOARD_DIR` 定位宿主打包器。本地 workspace、凭据、运行数据和生成的包不纳入版本跟踪，开发密钥仅用于测试宿主。

## 交付

按用户对检出环境的要求核对 Git 作者与提交者，使用仓库级身份配置。PR 指向负责该改动的分支，各长期分支保持线性历史。共享文档可单独同步；不将宿主分支合入 `main`，也不将分支拆分中的删除操作应用到宿主实现。

源码修改不自动发布插件版本。分别记录自动测试、宿主、目标平台和真实提供方验证。
