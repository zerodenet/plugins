# 仓库工作指南

[English](AGENTS.md) · **简体中文**

## 项目范围

修改前阅读 `README.md`、`CONTRIBUTING.md` 和 `docs/governance.md`；涉及发行版本时阅读 `docs/publishing.md`。

插件源码放在 `<host>/<plugin>/`，各自维护 manifest、依赖和测试。宿主实现与公开 SDK 留在各自仓库。保留插件 ID、发布者身份和已有历史，不创建嵌套 Git 仓库。

## 架构

市场元数据不授予宿主能力。各宿主负责权限检查、核心业务状态、私有存储和生命周期事务。插件使用专用、带版本的宿主 API，不引入数据库直连、任意宿主命令、管理员令牌或节点凭据。

共享市场不意味着运行时通用。ZBoard API 不进入客户端或 Zero 内核。新增能力应先提供宿主契约与验证，再由插件接入。

## 文档

英文为默认语言，公共文档应有对应的 `.zh-CN.md` 页面。提供双向语言链接，译文导航保持所选语言。行为、命令或要求变化时，同步更新两种语言。

文档面向用户与贡献者。本地迁移记录、工作站路径、作者账号设置和任务报告留在公共指南之外。技术限制放在相关参考页或架构文档中，未确定的契约标记为草案。

## 验证

从根目录运行 `sh scripts/check.sh`。文档修改还需检查链接、语言配对和命令示例。打包修改需显式选择宿主目录构建，并检查 manifest、签名和包文件清单。

使用 `--zboard` 或 `ZBOARD_DIR` 定位宿主打包器。`.local/`、密钥、数据库、生成的包和本地 Go workspace 不纳入版本跟踪。开发密钥仅供测试宿主使用。

## 交付

提交前检查暂存文件和实际 Git 作者、提交者身份。检出环境的身份要求使用仓库级配置，不修改全局身份。遵循 PR 流程并保持共享历史线性。

分别报告自动测试、宿主集成、目标平台和真实提供方验证。确认发布成功后，再声明目录或发行版本可用。源码修改不自动触发插件发行。
