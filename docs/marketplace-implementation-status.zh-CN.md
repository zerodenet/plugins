# 统一插件市场实施状态

[English](marketplace-implementation-status.md) · **简体中文**

本页对应 `marketplace-implementation-plan.zh-CN.md` 的代码落地证据。分支提交或推送不表示已经合并、部署或完成真实安装验收。

## 基线与迁移矩阵

| 范围 | 迁移前 | 本次落地 | 兼容退出条件 |
| --- | --- | --- | --- |
| 中央登记 | ZBoard 与 ZNet Sink 分开的 schema-v2 目录 | `catalogs/plugins.json` schema 3 统一按产品登记 | 两个受支持宿主版本都稳定使用统一 API，并走完公告兼容期 |
| 作者发行 | 作者仓库 `marketplace-entry.json`，既有单宿主格式 | 一个版本清单可带一到两个显式宿主目标；工具从签名包读取身份并计算大小/摘要 | 发布者完成清单迁移；快照构建期继续兼容既有清单 |
| 在线读取 | 宿主读取各自原始目录，并逐仓库发现发行 | 网站读取完整快照；宿主读取固定宿主/频道 JSON，并在本地按版本、系统和架构筛选 | 统一域名完成发布并通过真实宿主验收 |
| 本地安装 | 各宿主独立验包、授权和管理生命周期 | 保持不变；快照只负责发现和选择，不成为安装信任替代品 | 不退出 |

视觉依赖固定为 Minted Directory Astro 提交 `f71c8ae3fcff741285107415701fb6d1390f55b6`（MIT）。Ever Works 提交 `7d99ece8f06a99988721b83a9dbb878d7bfad7b8` 仅用于信息层级参考，其 AGPL 代码未复制；详见 `THIRD_PARTY_NOTICES.md`。

## 阶段结果

- [x] P0：完成脏工作树隔离、现网发行与两端消费路径盘点、上游提交与许可固定。
- [x] P1：完成产品注册 schema 3、发行清单 schema 1、迁移校验、Issue 首次入驻与资料更新自动化；Action 将固定清单中的产品字段原样写入审核 PR，宿主兼容投影由统一登记生成。
- [x] P2：完成注册模板、发布模板、双包清单生成、身份/能力/摘要/大小校验及负例测试；中央流程不执行插件。
- [x] P3：完成有边界的 GitHub 汇总、最近 20 条正式版/预发布动态、发布动态与已验证安装版本分离、最后可用数据回退、撤回优先、统一快照、六个宿主/频道静态 API、构建验证及客户端本地过滤测试。
- [x] P4：完成 Astro 列表、搜索、宿主与分类筛选、发布者登记值与已提交链接、发布记录、surfaces/capabilities 原 key 与宿主标准说明、评分/评价预留位、站内首次入驻与资料更新入口、文档站公开说明入口，以及无已验证产物时隐藏下载入口；安装、发布、安全与 API 说明统一归属 `docs.zerodenet.org/marketplace/`，市场仓库不保存第二份公开文案。产品字段不翻译、不改写、不推断缺失值，协议说明字典不包含插件专属覆盖，最终变更由用户登录 GitHub 确认并经 Action 生成审核 PR。已完成桌面与 390×844 移动视口检查。
- [ ] P5：ZBoard 与 ZNet Sink 的代码接入和兼容回退已完成，聚焦测试已通过；仍需部署后的真实签名包安装、升级和原数据保留 E2E，且没有双宿主真实包时不宣称双端验收。
- [ ] P6：定时/变更触发构建、审核产物、Pages 路径适配，以及网站、快照、六个宿主/频道 API 与 schema 的 GitHub Pages 自动发布已配置；仍需启用 Pages、合并到 `main`、首次线上验收，再配置 `plugins.zerodenet.org`，并完成缓存刷新和回滚演练。

## 当前发布边界

当前交付止于功能分支，不代表已合并或部署。首发不需要 Cloudflare 凭据；需要在 `zerodenet/plugins` 的 Pages 设置中一次性选择 GitHub Actions，合并后核验 GitHub Pages 地址，再把 `plugins.zerodenet.org` 配为自定义域名。Pages 首期同步发布静态网站、统一快照、`/api/plugins.json`、六个宿主/频道 JSON 和 schema。现有 schema-v2 投影继续可用，两个宿主在统一静态 API 不可用或返回无效响应时只读回退，不改变安装安全边界。
